<h1> Fabricantes </h1>
  <ul>
  <% if @manufacturers.empty? %>
  <h1>Nenhum fabricante cadastrado</h1>
  <% else %>
  <% @manufacturers.each do |m| %>
    <li>
      <%= link_to m.name, m %> <%#= manufacturer_path(id: m.id) %>
    </li>
    <%end%>
    <%= link_to 'Voltar', root_path %>
<% end %>